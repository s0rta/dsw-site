%section.main
  %section.content
    = render partial: 'layouts/shared/navigation'
    = render partial: 'layouts/shared/masthead'
    %hr
    -# %section.section-container.width-constrained
      -# %h2.primary-heading Sign In

    %section.section-container.module-form
      %div.form-carousel(data-bindable="dsw-carousel" data-keyboard="false" data-activate="0")
        %div.form-carousel-inner
          = simple_form_for(resource, as: resource_name, url: user_registration_path) do |f|
            %article.form-pane
              %div.form-contents
                %div.dswacon.dswacon-bulb.dswacon-combo-primary
                %h3.primary-heading Create an Account
                -# %div.p-copy Lorem ipsum dolere sit amet nonummy consecuter nomen es quid. Lorem ipsum dolere sit amet nonummy consecuter nomen es quid.

                - flash.each do |key, message|
                  %div.notification(class="notification-#{key}")= message

                %form.dsw-form(role="form")
                  %fieldset
                    = f.input :name, required: true, autofocus: true, placeholder: 'Name', label: false, error: true
                    = f.input :email, required: true, placeholder: 'Email', label: false, error: true
                    = f.input :password, required: true, placeholder: 'Password', label: false, error: true
                    = f.input :password_confirmation, required: true, placeholder: 'Confirm Password', label: false, error: true

              = f.button :submit, 'Sign Up', class: 'btn btn-primary form-btn'
              %div.p-copy
                = render "users/shared/links"
        %div.carousel-controls

= render partial: 'layouts/shared/footer'
= render partial: 'layouts/shared/flash_message'
= render partial: 'layouts/shared/spinner'
