header.masthead(data-bindable='toggle-nav')
  .section-content
    a#menu-icon href='#'
      = render partial: 'icons/hamburger'
    h1.logo-container
      a.logo href='/' <span>Denver Startup Week</span>

  nav.outer-modal-container
    .inner-modal-container
      a.close href='#'
        = render partial: 'icons/x_mark'
      - if signed_in?
        div
          = link_to 'My Dashboard', mine_submissions_path
      - elsif FeatureToggler.submission_active?
        div
          = link_to 'Submit Proposal', new_submission_path
      - else
        div
          = link_to 'Sign In', mine_submissions_path
      div
        = link_to 'Program', page_path(page: 'program')
      div
        = link_to 'Our Team', page_path(page: 'team')
      / div
        / = link_to 'Attendees', page_path(page: 'attendees')
      div
        = link_to 'Sponsors', page_path(page: 'sponsors')
      div
        = link_to 'Contact', page_path(page: 'contact')
      div
        = link_to 'FAQ', page_path(page: 'faq')
      - if signed_in?
        div
          = link_to 'Logout', destroy_user_session_path, method: :delete
