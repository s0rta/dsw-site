- color = track_color("#{@track[:name]}")
- backgroundColor = color[:backgroundColor]
- textColor = color[:textColor]

= render partial: 'layouts/shared/sub_program_details_hero', locals: { title: "#{@track[:name]}", subProgram: "Tracks", backUrl: "tracks", btnSessions: "Track", backgroundColor: backgroundColor, textColor: textColor }
= render partial: 'layouts/shared/track_list', locals: { tracks: @featured, trackName: "#{@track[:name].downcase}" }
div.LayoutWrapper
  = render partial: 'layouts/shared/primary_callouts_nav'